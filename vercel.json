{
    "version": 2,
    "buildCommand": "nest build",
    "outputDirectory": "dist",
    "functions": {
      "api/**/*.js": {
        "runtime": "nodejs18.x"
      }
    },
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/api/main.js"
      }
    ]
}